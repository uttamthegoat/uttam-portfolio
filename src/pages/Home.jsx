import transition from '../utils/transition'

const Home = () => {
  return (
    <div>
      <h1 className="h1s">Home</h1>
    </div>
  );
};

export default transition(Home)
// export default Home;
